#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char print[]=("with proper grammar, but the outcome isia piece of art,");
	printf("%s\n", print);
	return (0);
}
