#include "main.h"
/**
 * print_last_digit -  print the last digit
 *
 * Return: Always ld
 *
 * @num: variable
 *
 * @ld: variable
 */
int print_last_digit(int)
{
int num, ld;
ld = (num % 10);
if (num < 0)
{
ld = (-num % 10);
}
_putchar('0' + ld);
return (ld);
}
